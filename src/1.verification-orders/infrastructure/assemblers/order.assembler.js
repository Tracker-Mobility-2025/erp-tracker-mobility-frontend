/**
 * Assembler para Order (pendiente de implementación).
 * Infrastructure layer - DTO to Domain Entity mapper
 * 
 * TODO: Implementar cuando se defina la entidad Order
 */
export class OrderAssembler {
  static toEntity(resource) {
    throw new Error('OrderAssembler.toEntity: Pendiente de implementación');
  }

  static toEntities(resources) {
    throw new Error('OrderAssembler.toEntities: Pendiente de implementación');
  }
}
